apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: stackit-git
  namespace: argo
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    stackit-git-push:
      port: "12000"
      endpoint: /push
      method: POST
